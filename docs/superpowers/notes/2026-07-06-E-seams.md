# E Task Investigation: Seams for Critic Dispatch + Entropy Capture

## Status: DONE

This note confirms all the seams required for the E tasks are in place.

______________________________________________________________________

## 1a: Critic Auto-Spawn Chokepoint (Multica)

**File**: `multica/server/internal/service/task.go`

The chokepoints are:

- `CompleteTask` at line ~1296
- `FailTask` at line ~1487
- `CancelTask` at line ~924 (and `CancelTaskWithResult`)

These are the same chokepoints where D's `maybeCloseTrainingSession` should be attached.
Perfect for attaching `maybeSpawnCriticTask` instead when a critic is configured.

______________________________________________________________________

## 1b: Critic Task → Trained Session Linkage

**Migration**: `multica/server/migrations/003_task_context.up.sql` adds `context JSONB`
to `agent_task_queue`.

- This is the right home for
  `critic_of = {trained_task_id, proxy_key, session_id, project_id}`
- No existing field collision
- Close hook can read `context.critic_of` directly from the critic task without a join

______________________________________________________________________

## 1c: Critic Output Storage

**Table**: `agent_task_queue` has a `result JSONB` column (migration 001, line 137).

- Critic's output text lives in `result`
- Multica can parse `{"reward": <float>}` from the last line of the output
- Perfect for storing and retrieving the critic reward

______________________________________________________________________

## 1d: Env ID Availability

**File**: `multica/server/internal/service/env_dispatch.go`

- `EnvDispatchInput` has `EnvID string` at line 49
- Already passed through the dispatch flow
- Available when D's `maybeOpenTrainingSession` fires (T5 of D)
- Can thread through to training_dispatch and then to RL client

______________________________________________________________________

## 1e: AReaL Proxy Logprobs Capture

**File 1**: `areal/experimental/openai/proxy/proxy_rollout_server.py`

- `_call_client_create` at line 565 is the single chokepoint for all chat completions
  forwarding
- Perfect for injecting `logprobs=True`

**File 2**: `areal/experimental/openai/types.py`

- `InteractionWithTokenLogpReward` at line 36 can hold logprobs in
  `model_response.output_logprobs` (used in `to_tensor_dict` at line 191)
- `should_compute_prox_logp()` in agent config is orthogonal (it's for recomputing, not
  capturing)

______________________________________________________________________

## 1f: StartSessionRequest Env ID Field

**File**: `areal/experimental/openai/proxy/server.py`

- `StartSessionRequest` at line 26 can gain `env_id: str | None = None` additively (old
  callers still work)
- `SessionData` at line 66 can persist it without breaking existing serialization (just
  add an instance variable)

______________________________________________________________________

## Conclusion

All seams are confirmed! No blockers found.
